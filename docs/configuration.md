# Konfiguration und Logging

Diese Datei beschreibt Laufzeitkonfiguration, Umgebungsvariablen und Logging.

## Status

Platzhalter für die ausführliche Konfigurations-Dokumentation.

Geplant sind unter anderem:

- `.env` und benötigte Discord-Credentials
- `config.json` und projektspezifische Optionen
- `LOG_LEVEL` und `LOG_FORMAT`
- `traceId`-basiertes Logging